# DELTA NOTE — C5.8 (2d + Wire Move + Wire Save) — 13/07/2026
**Nguồn:** session Sonnet 13/07/2026, tiếp nối `C5.8_Wire_ExecutionPlan_12jul2026.md` v1.1
**Trạng thái:** Build + test node-level DONE (Phần 2, Card 1, Card 2). REG C5.8 (regression cuối) CHƯA chạy — làm sau khi phân phối doc này.
**Việc Claude Code cần làm:** đối chiếu diff trước commit, phân phối đúng từng mục vào file đích ghi ở đầu mỗi khối.

---

## 1 — CẬP NHẬT `docs/00_Core/DEVIATIONS.md`

```
[BUG-FIX] 13/07/2026 — OnRequestMoveFolder + CB_MoveCombo vẫn gọi BuildMoveFolderTargetList
  Rename Task Card #1 (08/07) từ BuildMoveFolderTargetList → BuildComboFolderTreeNodes
  KHÔNG propagate hết 2 call site (claim "Blueprint tự propagate qua rename" trong
  WBP_FurnitureInventory.md v3.9 SAI). Hệ quả: HasChildren/ChildCount luôn False/0 cho
  MỌI node trong Move dialog (hàm cũ sinh trước khi 2 field này tồn tại trong struct) →
  không hiện arrow/badge dù folder có con thật.
  Fix: OnRequestMoveFolder + CB_MoveCombo đổi sang gọi BuildComboFolderTreeNodes.
  BuildMoveFolderTargetList xoá hẳn (mồ côi sau fix, không giữ [LEGACY] — bản thiếu
  field, giữ lại chỉ gây nhầm lẫn).

[BUG-FIX] 13/07/2026 — SetSelectedHighlight dùng nhầm biến so sánh
  RefreshVisibleRows (WBP_FolderTreePicker): dòng gọi Row.SetSelectedHighlight() dùng
  CHUNG kết quả so sánh "===" với nhánh Set Current Tag (Break(Node).Path == CurrentPath)
  thay vì so với SelectedPath (biến riêng, đổi mỗi lần user click chọn).
  Hệ quả: click chọn folder → Confirm bấm được (SelectedTargetPath ở Dialog đúng) nhưng
  KHÔNG thấy highlight xanh (vì Picker so sai biến) — trừ khi trùng ngẫu nhiên CurrentPath.
  Fix: tách node so sánh riêng (Break(Node).Path == SelectedPath) cho nhánh
  SetSelectedHighlight, không dùng chung với nhánh tag "hiện tại".

[CORRECTION] 13/07/2026 — SetLabelColor nhận Slate Color, không phải Linear Color
  WBP_EditableLabel.SetLabelColor(InColor) — patch P2 (Card 0) giả định Linear Color vì
  suy loại theo Image widget. Thực tế Set Color and Opacity trên TextBlock (built-in UMG)
  nhận FSlateColor (struct bọc ngoài Linear Color). Không phải bug — Blueprint tự convert
  khi nối Linear Color var vào pin — chỉ sửa lại type khai báo param cho đúng bản chất API.

[SCOPE] 13/07/2026 — Rename qua context-menu trong picker KHÔNG tồn tại
  Test S6b (REG khối A) kỳ vọng right-click row → "Đổi tên" trong Move/Save dialog.
  Xác nhận: 2d (Task Card 2d) chỉ xây BeginRenameOnPath như hàm được GỌI TỪ CODE
  (trigger duy nhất: nút "+ Thư mục mới" ở Save dialog, sau khi CreateEmptyFolder).
  KHÔNG có UI right-click-để-rename trên row picker — đúng phạm vi 2d ban đầu, không
  phải thiếu sót. Nếu cần trigger này trong tương lai, cần task card riêng (ngoài C5.8).

[SCOPE] 13/07/2026 — Test mục 10 TC#2 gốc SUPERSEDED
  "Click tên → UI không đổi" (TC#2 cũ) không còn đúng sau Card 1 — click chọn giờ PHẢI
  đổi UI (highlight xanh). Hành vi mới đúng ý, chỉ ghi nhận thay đổi kỳ vọng test.

[CEILING] 13/07/2026 — Bind OnFolderSelected trong InitPicker giả định 1 lần/instance
  WBP_MoveToFolderDialog.InitPicker Bind Picker.OnFolderSelected → HandlePickerFolderSelected
  ngay trong hàm InitPicker (không phải Event Construct). An toàn CHỈ KHI mỗi lần mở dialog
  là 1 instance mới (Create Widget mới, đúng hiện trạng). Nếu sau này đổi sang tái dùng
  instance (gọi InitPicker nhiều lần/1 instance) → Bind sẽ chồng (double-fire) → phải dời
  sang Event Construct lúc đó.

[SCOPE] 13/07/2026 — 6A của Create Folder (Save dialog)
  User tạo folder mới rồi ESC/blur trước khi đặt tên → WBP_EditableLabel tự revert về
  "New Folder (N)" (đã có sẵn cơ chế). Folder rỗng tên auto này vẫn tồn tại trong
  Folders.json — CHẤP NHẬN, giống hành vi NF (New Folder qua context-menu tree chính)
  đã có từ trước. Không phải bug, không cần cleanup ở C5.8 — nếu cần dọn folder rỗng
  tên tự động sau này thì đưa vào C10 (regression tổng).

[CLEANUP] 13/07/2026 — Print debug ở HandleSaveDialogCreateFolder/HandleSavePickerRenameCommitted
  Dùng EnabledState=DevelopmentOnly (property node, tự strip khỏi Shipping build) thay vì
  gate bằng biến bDebugMode như quy ước project. Quyết định giữ nguyên (đơn giản hơn, đủ
  hiệu quả) — không đổi. Ghi nhận khác biệt cơ chế: DevelopmentOnly cắt theo LOẠI BUILD,
  bDebugMode cắt theo RUNTIME TOGGLE (bật/tắt không cần rebuild).
```

---

## 2 — CẬP NHẬT `docs/Widgets/WBP_FolderPickerRow.md` → bump **v1.2 → v1.3**

### Thêm mới (Task Card 2d, Phần 1)

**1a — Designer:** `TXT_Name` (TextBlock) → thay bằng `EditableLabel_Name` (`WBP_EditableLabel`, Is Variable=✓), vị trí giữ nguyên trong `HB_Label`, trước `TXT_Badge`.

**1b — `SetNode` sửa:**
```
Set Text(TXT_Name, ...) → EditableLabel_Name.SetLabel(Node.DisplayLabel)
```

**P2 (chốt cách B — relay function, không đục thẳng widget con):**
```
### SetLabelColor(InColor : Slate Color)   [WBP_EditableLabel, MỚI — xem mục 3]
```
`SetSearchHighlight` sửa lại nhánh gọi:
```
Branch(Match)
  True  → EditableLabel_Name.SetLabelColor(GET Color Match)
  False → EditableLabel_Name.SetLabelColor(GET Color Default)
```

**1c — `EnterRenameMode(Siblings : Array<String>)` — Custom Event (relay):**
```
▶ EditableLabel_Name.EnterEditMode(Siblings)
```

**1d — Event Construct, thêm bind:**
```
Bind EditableLabel_Name.OnLabelRenameCommitted → HandleLabelCommitted
```

**1e — `HandleLabelCommitted(NewName : String)` — Custom Event (bound):**
```
▶ Broadcast OnRowRenameCommitted(RowNode.Path, NewName)
```

**1f — Dispatcher mới:**
```
OnRowRenameCommitted(Path : String, NewName : String)
```

**P3 — Getter mới:**
```
### GetRowPath() → String   (Pure)
▶ Return = GET RowNode → Break → Path
```
Xác nhận qua export K2Node (screenshot cuhoang) — khớp spec, exec pin Entry→Return bình thường dù Pure (chỉ ẩn ở node GỌI từ ngoài).

### Test PASS (Task Card 2d Phần 1, xác nhận trực tiếp qua thao tác row + qua Move dialog thật)
```
1. EnterRenameMode → text bôi đen, EditBox nhận focus         ✅
2. Gõ tên hợp lệ + Enter → OnRowRenameCommitted fire đúng      ✅
3-4. Validate tên trùng/ký tự "/" (relay từ WBP_EditableLabel) — kế thừa, không test riêng
5. Click ra ngoài → revert, không fire commit (6A)             ✅ (kế thừa từ WBP_EditableLabel)
6. Tên tiếng Việt có dấu → commit đúng                          ✅
0.2 Hồi quy search: SetLabelColor (Slate Color) không vỡ highlight vàng  ✅
0.3 Hồi quy hiển thị: expand/collapse không vỡ tên/badge/arrow  ✅ (test trong Move/Save dialog, xác nhận 13/07)
```

---

## 3 — CẬP NHẬT `docs/Widgets/WBP_EditableLabel.md` → bump **v1.0 → v1.1**

### Thêm mới
```
### SetLabelColor(InColor : Slate Color)
▶ Set Color and Opacity(Target=TextBlock_Label, In Color and Opacity=InColor)
```
> `[CORRECTION]`: param type đúng là **Slate Color**, không phải Linear Color như patch gốc ghi — do `Set Color and Opacity` trên TextBlock (built-in UMG) khác API so với Image widget. Blueprint tự convert khi nối Linear Color var vào, không ảnh hưởng runtime.

---

## 4 — CẬP NHẬT `docs/Widgets/WBP_FolderTreePicker.md` → bump **v1.2 → v1.3**

### Thêm mới (Task Card 2d Phần 2 + Card 1 + bug fix)

**Var mới:**
| Tên | Kiểu | Default |
|---|---|---|
| `CurrentPath` | String | "" |
| `bShowCurrentTag` | Boolean | false |

**Dispatcher mới:**
```
OnRequestCommitRename(OldPath : String, NewName : String)
OnFolderSelected(Path : String)   ← đã có, xác nhận lại thật (không còn ⚠️ SUY LUẬN)
```

**`BeginRenameOnPath(Path : String, Siblings : Array<String>)` — Function MỚI:**
```
Local: Children, RowRef, bFound
▶ SET Children = Get All Children(SB_Rows)
▶ SET bFound = False
▶ ForEachLoopWithBreak(Children):
     Cast Element → WBP_FolderPickerRow → RowRef
       Cast OK → Branch(RowRef.GetRowPath() == Path)
                    True  → RowRef.EnterRenameMode(Siblings) → SET bFound=True → Break
                    False → tiếp loop
       Cast Failed → tiếp loop
   Completed → Branch(NOT bFound) True → Print "không tìm thấy row" [DevelopmentOnly]
```

**`RefreshVisibleRows` — sửa, thêm relay bind:**
```
Bind Row.OnRowRenameCommitted → HandleRowRenameCommitted   ← chỗ bind mỗi row đã có
```

**`HandleRowRenameCommitted(Path, NewName)` — Custom Event MỚI (bound):**
```
▶ Broadcast OnRequestCommitRename(Path, NewName)
```

**`ExpandToPath(Path : String)` — Function MỚI (Card 1):**
```
Local: Segments, AccumPath
▶ ParseIntoArray(Path, "/", CullEmpty=✓) → SET Segments
▶ SET AccumPath = ""
▶ ForLoop(0 → Segments.LastIndex):        ← chạy TỚI HẾT (khác BuildSearchOverride n-2, chủ đích)
     Branch(Index==0) True→ AccumPath=Segments[0]
                      False→ AccumPath=Concat(AccumPath,"/",Segments[Index])
     [merge] → Array_AddUnique(ExpandedFolders, AccumPath)
Completed → hết (KHÔNG tự gọi RefreshVisibleRows — caller quyết định)
```

**`RefreshVisibleRows` — sửa, NGAY SAU `Row.SetNode(node)`:**
```
Row.SetCurrentTag( bShowCurrentTag AND (node.Path == CurrentPath) )
Row.SetSelectedHighlight( node.Path == SelectedPath )   ← [BUG-FIX] TÁCH RIÊNG so sánh
                                                            (trước fix: dùng chung "===" với
                                                            nhánh CurrentTag, so nhầm CurrentPath)
```

**`HandleRowSelected(Path)` — sửa (xác nhận thật, xoá cờ ⚠️ SUY LUẬN):**
```
HandleRowSelected(Path):
  ▶ SET SelectedPath = Path
  ▶ RefreshVisibleRows()
  ▶ Broadcast OnFolderSelected(Path)
```
Thứ tự bắt buộc: `RefreshVisibleRows` TRƯỚC `Broadcast` — vỏ nhận event khi UI đã render highlight xong.

### Test PASS
```
Phần 2 test 1, 2 (BeginRenameOnPath tìm đúng row + commit qua Picker)   ✅
Test 6 (path không tồn tại) — CHƯA TEST, rủi ro thấp, defer
M1-M6 (Wire Move full flow, mirror REG A1-A2)                            ✅ tất cả PASS
  M3 lúc đầu FAIL (highlight sai biến) → fix → PASS lần 2
0.3 hồi quy hiển thị (ExpandAll/CollapseAll + expand/collapse lẻ)       ✅
```

---

## 5 — CẬP NHẬT `docs/Widgets/WBP_FolderPickerRow.md` (bổ sung — Card 1)

**2 hàm mới (Custom Event):**
```
### SetCurrentTag(bVisible : Boolean)
Branch(bVisible) True→ Set Visibility(TXT_CurrentTag, Visible)
                 False→ Set Visibility(TXT_CurrentTag, Collapsed)

### SetSelectedHighlight(bSelected : Boolean)
Branch(bSelected) True→ Set Background Color(BTN_Name, R=0.2 G=0.4 B=1.0 A=0.4)
                  False→ Set Background Color(BTN_Name, R=1 G=1 B=1 A=1)
```
Designer: thêm `TXT_CurrentTag` (TextBlock, text="hiện tại", Collapsed mặc định) vào `HB_Label`, SAU `TXT_Badge`.

---

## 6 — CẬP NHẬT `docs/Widgets/WBP_MoveToFolderDialog.md` → bump **v1.0 → v2.0**

Xoá hẳn: `ScrollBox_FolderList`, `WBP_MoveFolderRow` reference, `PopulateRows`, `HandleRowSelected` (bản cũ của Dialog), var `CurrentSelectedRow`.

Thêm mới:
```
Picker : WBP_FolderTreePicker (Is Variable=✓)

### InitPicker(Entries : Array<S_FolderTreeNode>, InCurrentPath : String, bInShowTag : Boolean)
▶ SET Picker.CurrentPath = InCurrentPath
▶ SET Picker.bShowCurrentTag = bInShowTag
▶ Picker.ExpandToPath(InCurrentPath)
▶ Picker.SetFolders(Entries)
▶ SET SelectedTargetPath = "" | bHasSelection = False
▶ Set Is Enabled(BTN_Confirm, False)
▶ Bind Picker.OnFolderSelected → HandlePickerFolderSelected
> [CEILING] Bind trong InitPicker giả định 1 lần/instance — xem DEVIATIONS mục 1.

### HandlePickerFolderSelected(Path : String) — Custom Event
▶ SET SelectedTargetPath = Path | bHasSelection = True
▶ Set Is Enabled(BTN_Confirm, True)
```
`BTN_Confirm`/`BTN_Cancel` — giữ nguyên 100%.

**`WBP_MoveFolderRow`** — SUPERSEDED, không xoá file (đánh dấu tương tự pattern DA_FurnitureItem).

### Test PASS: M1-M6 (xem mục 4)

---

## 7 — CẬP NHẬT `docs/Widgets/WBP_SaveComboDialog.md` → bump **v1.0 → v2.0**

Xoá hẳn: `CMB_Folder`, `BTN_NewFolder`, `TextBox_FolderPath`, `HB_Folder_Row`, var `bIsCreatingNewFolder`, pin `ExistingFolders` (Expose on Spawn), đoạn Event Construct cũ (`CLEAR Options CMB_Folder` + `ForEach ExistingFolders`).

Thêm mới:
```
Picker : WBP_FolderTreePicker (Is Variable=✓, SizeBox ~180px cao, compact)
BTN_AddFolder : Button (text "+ Thư mục mới")

Dispatcher mới: OnRequestCreateFolder(ParentPath : String)

BTN_AddFolder.OnClicked ▶→ Broadcast OnRequestCreateFolder(Picker.SelectedPath)

BTN_Confirm — nhánh folder đổi:
  (Branch bIsCreatingNewFolder cũ) → GET Picker.SelectedPath → SET TempFolder
```
`TagVocabulary`, field Name/Description/Tags, `ValidateComboName`, `ParseTags` — giữ nguyên.

### Test PASS: S6a, S6c
### Test [SCOPE — không áp dụng]: S6b (context-menu rename không tồn tại theo thiết kế)

---

## 8 — CẬP NHẬT `docs/Widgets/WBP_FurnitureInventory.md` → bump version (Sonnet để Claude Code xác nhận số version tiếp theo)

### `OnRequestMoveFolder(FolderPath)` — sửa
```
BuildComboFolderTreeNodes(FolderPath) → Entries   ← [BUG-FIX] thay BuildMoveFolderTargetList
Create Widget(WBP_MoveToFolderDialog) → Dialog
Dialog.InitPicker(Entries, ParentOf(FolderPath), True)
(Bind OnMoveFolderConfirmed + AddToViewport + Input Mode — giữ nguyên)
```

### `CB_MoveCombo` — sửa
```
BuildComboFolderTreeNodes("") → Entries           ← [BUG-FIX] thay BuildMoveFolderTargetList
Dialog.InitPicker(Entries, MovingComboCurrentFolder, True)
(phần còn lại giữ nguyên)
```

### `BuildMoveFolderTargetList` — **XOÁ HẲN** (mồ côi sau 2 fix trên, không giữ [LEGACY])

### `OpenSaveComboDialog` — sửa (sau `SET SaveComboDialogRef`, TRƯỚC `AddToViewport`)
```
Branch(IsValid(SaveComboDialogRef))
  True →
    BuildComboFolderTree()
    BuildComboFolderTreeNodes("") → Entries
    Dialog.Picker.SetFolders(Entries)
    SET Dialog.Picker.bShowCurrentTag = False
    Bind Dialog.OnRequestCreateFolder → HandleSaveDialogCreateFolder
    Bind Dialog.Picker.OnRequestCommitRename → HandleSavePickerRenameCommitted
  False → dead-end (guard có sẵn từ trước C5.8, không phải patch mới)
(XOÁ đoạn cũ: GetExistingFolders → SET TempFolders, đã orphan sau khi pin ExistingFolders
 bị xoá khỏi WBP_SaveComboDialog Expose on Spawn)
```

### `HandleSaveDialogCreateFolder(ParentPath : String)` — Custom Event MỚI
```
GetUniqueNewFolderName(ParentPath) → NewName
Select(String)(Pick=ParentPath=="", A=NewName, B=Concat(ParentPath,"/",NewName)) → SET SaveDlg_NewFolderPath
CreateEmptyFolder(SaveDlg_NewFolderPath) → bOK
Branch(bOK)
  False → Print "CreateEmptyFolder failed: "+SaveDlg_NewFolderPath [DevelopmentOnly]
  True  →
    BuildComboFolderTree() → BuildComboFolderTreeNodes("") → Entries
    Branch(IsValid(SaveComboDialogRef))
      True →
        Picker.SetFolders(Entries)
        Picker.ExpandToPath(SaveDlg_NewFolderPath)
        Picker.RefreshVisibleRows()
        GetSiblingFolderNames(SaveDlg_NewFolderPath) → Siblings
        Picker.BeginRenameOnPath(SaveDlg_NewFolderPath, Siblings)
      False → dead-end
```
Var mới: `SaveDlg_NewFolderPath : String` (class var, prefix `SaveDlg_`).

### `HandleSavePickerRenameCommitted(OldPath, NewName)` — Custom Event MỚI
```
ParentOf(OldPath) → ParentPath
Select(String) hoặc Branch(ParentPath=="") → NewFullPath
RenameFolderPrefix(OldPath, NewFullPath) → (Return Value KHÔNG dùng để gate — xem ghi chú)
BuildComboFolderTree() → BuildComboFolderTreeNodes("") → Entries
Branch(IsValid(SaveComboDialogRef))
  True →
    Picker.SetFolders(Entries)
    Picker.ExpandToPath(NewFullPath)
    SET Picker.SelectedPath = NewFullPath
    Picker.RefreshVisibleRows()
  False → dead-end
```
> **Ghi chú quan trọng — KHÔNG Branch theo `RenameFolderPrefix` Return Value (Integer, đếm combo bị đổi):**
> Verify code C++ thật (`ComboSerializer::RenameFolderPrefix`, có nhánh C5.3 cascade sang
> `Folders.json` manifest ĐỘC LẬP với count combo) — `Count=0` (rename folder rỗng) vẫn
> cập nhật registry đúng qua nhánh cascade. Gate theo `Count>0` sẽ sai (báo fail nhầm cho
> case phổ biến nhất — rename ngay sau khi tạo folder rỗng). Chạy thẳng không Branch,
> đúng spec gốc Wire_ExecutionPlan.

Var mới: `SaveDlg_RenamedPath` không dùng — thực tế as-built dùng `NewFullPrefix`/pattern tương tự `HandleMoveFolderConfirmed` cũ (xem export K2Node thật để ghi chính xác tên node nếu cần, Sonnet ghi theo mô tả logic, tên biến thật có thể lệch nhẹ — Claude Code đối chiếu export nếu cần chính xác tuyệt đối).

### Test PASS: S6a, S6c, M1-M6, Phần 2 test 1-2

---

## 9 — CẬP NHẬT `docs/00_Core/PROGRESS.md`

Thêm dòng:
```
| C5.8 | Folder Tree Picker Unify — 2d (rename host) + Wire Move + Wire Save hoàn tất build
         + trả nợ test toàn bộ (M1-M6, S6a-c, 0.3, Phần 2 test 1-2). 3 bug fix phát hiện
         lúc test (BuildMoveFolderTargetList sót 2 call site, SetSelectedHighlight sai biến,
         SetLabelColor type correction). REG C5.8 (regression cuối) CHƯA chạy — bước tiếp theo. | 🔄 IN PROGRESS (13/07/2026) |
```

---

## 10 — CẬP NHẬT `docs/00_Core/01_Session_State.md`

Thêm dòng mới nhất:
```
| 13/07/2026 | **C5.8 — 2d + Wire Move + Wire Save build + test DONE**. 3 bug fix: (1)
OnRequestMoveFolder/CB_MoveCombo sót gọi BuildMoveFolderTargetList (hàm cũ, thiếu
HasChildren/ChildCount) — đổi sang BuildComboFolderTreeNodes, xoá hàm cũ; (2)
SetSelectedHighlight so sai biến (CurrentPath thay SelectedPath) — tách so sánh riêng;
(3) SetLabelColor type Slate Color không phải Linear Color. Test PASS: Phần 2 (1,2),
0.3, M1-M6, S6a/S6c. S6b [SCOPE] không áp dụng (context-menu rename không tồn tại theo
thiết kế 2d). Version bump: WBP_FolderPickerRow v1.3, WBP_FolderTreePicker v1.3,
WBP_EditableLabel v1.1, WBP_MoveToFolderDialog v2.0, WBP_SaveComboDialog v2.0,
WBP_FurnitureInventory (bump tiếp theo). TIẾP THEO: REG C5.8 (task card chốt sổ,
khối A/B/C) → sau đó mới cho phép sang C9. |
```

---

## Việc còn treo — KHÔNG nằm trong scope delta note này
- REG C5.8 (khối A/B/C, task card `C5.8_REG_TaskCard_11jul2026.md`) — chưa chạy.
- Phần 2 test 6 (BeginRenameOnPath, path không tồn tại) — rủi ro thấp, có thể gộp vào REG.
- Xác nhận tên biến chính xác trong `HandleSavePickerRenameCommitted` qua export K2Node thật nếu Claude Code cần mức chính xác tuyệt đối cho doc (Sonnet ghi theo logic đã verify qua screenshot, không phải export đầy đủ 100% cho hàm này).
