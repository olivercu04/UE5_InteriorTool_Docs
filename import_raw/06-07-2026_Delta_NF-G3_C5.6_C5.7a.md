# DELTA — NF.G3 + C5.6 + C5.7a (nút "+" / Xóa folder / ChipTag right-click)
**Ngày:** 06/07/2026 | **Tác giả:** Sonnet (session hôm nay) | **Base:** Session_State.md v04/07/2026 22:10 ICT

> File này là delta — chỉ chứa PHẦN THAY ĐỔI so với canonical docs hiện tại.
> Claude Code: đọc từng mục, áp vào đúng file đích, KHÔNG tự ý diễn giải thêm.
> cuhoang review diff trước khi commit như thường lệ.

---

## 0. TÓM TẮT (đọc 30 giây)

Hoàn thành 3 việc trong backlog C5 (theo thứ tự Session_State 04/07):
- ✅ **NF.G3** — nút "+" đầu cột tree (tạo folder TRONG folder đang xem)
- ✅ **C5.6** — Xóa folder (WBP_ConfirmDialog mới + ClearFolderPrefix)
- ✅ **C5.7a** — ChipTag right-click → context menu (tái dùng OnComboTreeNodeRightClicked)

Phát sinh ngoài kế hoạch — **4 bug tìm & fix trong lúc test**, **1 hàm mới** (`RefreshChipBreadcrumb` + `RebuildChipRowForPath`), **1 deviation UX** (điều hướng sau xóa folder).

**C5 còn lại:** C5.7b (inline rename trong chip, nhúng WBP_EditableLabel) → REG (regression 12 bước) → C5 HOÀN TẤT.

---

## 1. NF.G3 — Nút "+" đầu cột tree ✅ DONE

**File đích:** `docs/Widgets/WBP_FurnitureInventory.md` (bump version), `docs/Blueprints/Blueprint_Logic_NodeFlow.md`

### PopulateComboTreeColumn() — thêm khối ĐẦU TIÊN (trước node "Tất cả")
```
Clear Children(VerticalBox_44)

// ══ MỚI — node "+" luôn đứng đầu ══
Create Widget(WBP_TreeNode) → PlusNode
SET PlusNode.FolderPath = "__NEWFOLDER__"
SET PlusNode.FolderName = "+ Tao folder"
SET PlusNode.IndentLevel = 0
RefreshDisplay(bIsActive = false)
Add Child(VerticalBox_44, PlusNode)
Bind OnNodeSelected(PlusNode) → OnComboTreeNodeClicked
(KHÔNG bind OnNodeRightClicked — né context menu trên hàng "+")

// ── phần cũ giữ nguyên, chạy tiếp sau khối trên ──
```

### OnComboTreeNodeClicked(SelectedPath, IndentLevel) — thêm guard ĐẦU TIÊN
```
Entry
▶→ Branch(SelectedPath == "__NEWFOLDER__")
     True  ▶→ OnRequestNewFolder( GetNewFolderParent() )   ← KẾT THÚC lần gọi
     False ▶→ ...logic cũ giữ nguyên...
```

**Không tạo hàm mới** — `GetNewFolderParent()` và `OnRequestNewFolder(ParentPath)` đã có sẵn từ NF context-menu (04/07), chỉ thêm 1 điểm gọi mới.

**Khác biệt hành vi so với context-menu "Create New Folder":** nút "+" tạo folder TRONG folder đang xem (`GetNewFolderParent`), còn context-menu tạo CÙNG CẤP node bị right-click (`ParentOf`).

**Test PASS 5/5:** tạo ở gốc, tạo trong folder active, right-click hàng "+" không mở menu, tạo trùng tên tự tăng hậu tố, click "+" không đè lên hành vi khác.

---

## 2. C5.6 — Xóa folder ✅ DONE

**File mới:** `docs/Widgets/WBP_ConfirmDialog.md` (tạo mới)
**File đích:** `docs/Widgets/WBP_FurnitureInventory.md` (bump version)

### WBP_ConfirmDialog (widget mới, generic — dùng lại được)
```
Root Overlay
└─ Border_Backdrop (full-screen, đen alpha ~0.4)
   └─ Border_Panel (center, ~380×160)
      └─ VerticalBox
         ├─ TXT_Message (multi-line)
         └─ HorizontalBox [BTN_Cancel "Hủy"] [BTN_Confirm]
```
Expose on Spawn: `Message : Text`, `ConfirmLabel : Text`.
Dispatcher: `OnConfirmed` (không param).

```
Event Construct:
  SetText(TXT_Message, Message)
  SetText(BTN_Confirm's TextBlock, ConfirmLabel)
  Set Keyboard Focus(self)

BTN_Confirm.OnClicked  ▶→ Broadcast OnConfirmed ▶→ Remove from Parent
BTN_Cancel.OnClicked   ▶→ Remove from Parent
OnKeyDown override:
  Key == Escape ▶→ Remove from Parent ▶→ Return Handled
  else          ▶→ Return Unhandled
```

> ⚠️ **BUG FIX (Class Defaults):** `bIsFocusable` phải TICK = true trên WBP_ConfirmDialog, nếu không `Set Keyboard Focus(self)` không có tác dụng — UMG mặc định `bIsFocusable = false`, gây warning "does not support focus" và Esc không hoạt động. Áp dụng cho MỌI widget dùng `Set Keyboard Focus(self)` ở cấp UserWidget (không áp dụng cho widget native con như EditableTextBox — tự nó đã focusable).

### WBP_FurnitureInventory — class var mới
```
PendingDeleteFolderPath : String (default "")
```

### OnRequestDeleteFolder(FolderPath) — implement (thay STUB)
```
SET PendingDeleteFolderPath = FolderPath
Create Widget(WBP_ConfirmDialog) → Dialog
  Message = "Xóa folder '" + LastSegmentOf(FolderPath)
          + "'? Combo bên trong sẽ chuyển về Chưa phân loại (không bị xóa)."
  ConfirmLabel = "Xóa"
Bind Dialog.OnConfirmed → HandleDeleteFolderConfirmed
Add to Viewport
Get Player Controller → Set Input Mode UI Only
```

### HandleDeleteFolderConfirmed() — Custom Event mới
```
Get Player Controller → Set Input Mode Game and UI
Branch( CurrentComboFolderPath == PendingDeleteFolderPath
        OR CurrentComboFolderPath StartsWith (PendingDeleteFolderPath + "/") )
     True  ▶→ ParentOf(PendingDeleteFolderPath) → ParentPath
            ▶→ Branch(ParentPath == "")
                 True  ▶→ SET CurrentComboFolderPath = "__ALL__"
                 False ▶→ SET CurrentComboFolderPath = ParentPath
     False → (merge, không đổi)
▶→ UComboSerializer::ClearFolderPrefix(PendingDeleteFolderPath) ●→ ChangedCount
▶→ RefreshComboFolderUI()
▶→ Print String("Đã xóa folder, " + ChangedCount + " combo về Chưa phân loại")   [gate bDebugMode]
▶→ SET PendingDeleteFolderPath = ""
```

> **[DEVIATION D-C5.6-1]** Spec gốc (`Combo_C5_FolderManagement_Plan_v2.md`, test case 5) quy định "nhảy về `__ALL__`" khi xóa folder đang xem/cha của đang xem. Đổi thành **"nhảy về folder CHA trực tiếp"** (dùng `ParentOf` có sẵn từ C5.2) — nếu cha là gốc (`""`) thì mới về `__ALL__`. Lý do: UX mượt hơn khi xóa folder lồng sâu (`Cha/Con1/Con1-2`) — giữ ngữ cảnh thay vì bật về Tất cả mỗi lần xóa. Phát hiện qua test thực tế, cuhoang duyệt hướng "về cha trực tiếp".

### CB_DeleteFolderClick — implement (thay STUB, bind Item3 trong OnComboTreeNodeRightClicked)
```
GET LibraryMenuRef → IsValid →
  LibraryMenuRef.Hide
  → OnRequestDeleteFolder(LibraryMenuRef.TargetFolderPath)   ← đọc TRƯỚC khi None
  → SET LibraryMenuRef = None                                ← cuối chuỗi, khớp pattern CB_CreateNewFolder/CB_MoveFolderClick
```

> ⚠️ **BUG FIX (thứ tự exec):** Lúc đầu code SET `LibraryMenuRef = None` TRƯỚC khi đọc `TargetFolderPath` → Accessed None runtime error. Đã sửa: đọc property trước, SET None sau cùng.
>
> **Ghi chú tồn kho:** `CB_RenameFolder` (từ C5.2) là ca lẻ loi KHÔNG có `SET LibraryMenuRef = None` sau `Hide` — không gây lỗi hiện tại nhưng lệch pattern so với 4/5 CB_ khác. Backlog dọn sau, không sửa trong phạm vi C5.6 (KP3).

**Test PASS 6/6:** xóa có combo, xóa có con lồng, xóa rỗng, Hủy, đang xem folder bị xóa (nhảy về cha), reload không hồi sinh.

---

## 3. C5.7a — ChipTag right-click → context menu ✅ DONE

**File đích:** `docs/Widgets/WBP_ChipTag.md` (tạo mới nếu chưa có, hoặc thêm vào WBP_FurnitureInventory.md), `docs/Widgets/WBP_FurnitureInventory.md`

### WBP_ChipTag — Dispatcher mới + override On Mouse Button Down
```
Dispatcher mới: OnChipRightClicked(FolderPath : String)

On Mouse Button Down(MyGeometry, MouseEvent) — override:
  Get Effecting Button(MouseEvent) → ReturnValue
  Branch(ReturnValue == Right Mouse Button)
    True  → Broadcast OnChipRightClicked(GET FolderPath_ChipTag)
             Return Node ← Make Event Reply(Handled)
    False → Return Node ← Make Event Reply(Unhandled)   ← cho Button_ChipTag xử lý OnClicked bình thường
```

> Pattern copy y hệt `WBP_TreeNode.On Mouse Button Down` (v1.2). Khác biệt: `WBP_ChipTag` root là `Horizontal Box` (chứa `Button_ChipTag` con), không phải chính node là Button như `WBP_TreeNode` — nên nhánh `False` PHẢI trả `Unhandled` (không có ở TreeNode vì root nó chính là Button).

### Bind ở 2 điểm tạo WBP_ChipTag trong WBP_FurnitureInventory
Thêm cạnh dòng `Bind OnChipSelected(NewChip) → OnComboChipTagClicked` đã có sẵn, ở CẢ 2 chỗ (`OnComboTreeNodeClicked` nhánh tạo ChipRow cấp 2, và `RebuildChipRowForPath` — xem mục 4):
```
Bind OnChipRightClicked(NewChip) → OnComboTreeNodeRightClicked   ← tái dùng handler tree, signature khớp (FolderPath : String)
```

**Không cần logic mới** — `OnComboTreeNodeRightClicked` đã có sẵn 4 menu item + guard sentinel, dùng chung cho cả tree node và chip.

**Test PASS 3/4 case ban đầu:** right-click chip mở menu đúng path (PASS), move/xóa từ chip chạy đúng (PASS), click trái chip vẫn chọn folder bình thường (PASS). Rename từ chip → **FAIL theo dự kiến** (đây là phần C5.7b, `OnRequestRenameFolder` chưa biết tìm trong `VB_ChipTagArea`, chưa làm).

---

## 4. BUG FIX phát sinh — Chip area không tự refresh sau Move/Xóa/Rename folder

**Phát hiện lúc test C5.7a case 3** (move combo từ chip → chip area không tự vẽ lại, phải chuyển folder khác rồi quay lại).

**Nguyên nhân gốc:** `RefreshComboFolderUI()` chỉ gọi `PopulateComboTreeColumn()` (vẽ lại `VerticalBox_44`), không có gì vẽ lại `VB_ChipTagArea` — cột chip chỉ được dựng tại thời điểm click, không ai chủ động rebuild khi dữ liệu đổi từ nguồn khác (move/rename/xóa).

### Function mới — `RebuildChipRowForPath(Path : String, OwnIndentLevel : Integer)`
Tách logic dựng 1 hàng chip (trước đây lặp lại 2 lần trong `OnComboTreeNodeClicked` và `OnComboChipTagClicked`) thành hàm dùng chung:
```
Map Find(ComboFolderTree, Path) → ChildCSV, bFound
Branch(bFound):
  False → return (không có gì để vẽ)
  True  → Create Widget(WBP_ChipRow) → Row
           SET Row.RowIndentLevel = OwnIndentLevel + 1
           Parse Into Array(ChildCSV, ",") → ChildNames
           ForEach ChildNames (element):
             Create Widget(WBP_ChipTag) → NewChip
             SET NewChip.FolderPath_ChipTag = Append(Path, "/", element)
             SET NewChip.FolderName_ChipTag = element
             SET NewChip.IndentLevel_ChipTag = OwnIndentLevel + 1
             Bind OnChipSelected(NewChip) → OnComboChipTagClicked
             Bind OnChipRightClicked(NewChip) → OnComboTreeNodeRightClicked
             AddChild(Row.HorizontalBox_ChipRow, NewChip)
           Completed → AddChild(VB_ChipTagArea, Row)
```
`OnComboTreeNodeClicked` và `OnComboChipTagClicked` — thay khối "Create WBP_ChipRow...Completed AddChild" trùng lặp bằng lời gọi hàm này.

### Function mới — `RefreshChipBreadcrumb()`
Dựng lại TOÀN BỘ chuỗi hàng chip từ `CurrentComboFolderPath`, gọi từ `RefreshComboFolderUI` sau `UpdateComboFolderHighlights` (cả 3 nhánh filter):
```
ClearChildren(VB_ChipTagArea)
Branch(CurrentComboFolderPath == "__ALL__" OR CurrentComboFolderPath == ""):
  True → return
Parse Into Array(CurrentComboFolderPath, "/") → Segments   ← Delimiter PHẢI là "/" (1 ký tự)
Branch(Segments.Length <= 1):
  True → return          ← cấp 1 (root) — tree tự vẽ cấp 2 lồng, không cần chip

Local: BuildPath = Append(Segments[0], "/", Segments[1])   ← path cấp 2
Local: Lvl = 1
RebuildChipRowForPath(BuildPath, Lvl)                       ← LUÔN vẽ hàng đầu

Branch(Segments.Length > 2):
  False → return
  True  → ForLoop(2, Segments.Length - 1):
            SET BuildPath = Append(BuildPath, "/", Segments[Index])
            SET Lvl = Lvl + 1
            RebuildChipRowForPath(BuildPath, Lvl)
          Completed → end
```

### RefreshComboFolderUI() — nối thêm RefreshChipBreadcrumb ở CẢ 3 NHÁNH
```
RefreshComboFolderUI():
  LoadComboLibrary
  BuildComboFolderTree → PopulateComboTreeColumn
  Branch(3 nhánh filter hiện có — __ALL__ / "" / path thật)
  → UpdateComboFolderHighlights()   [mỗi nhánh]
  → RefreshChipBreadcrumb()          ← THÊM, nối cả 3 nhánh (trước đây chỉ 1/3 nhánh có)
```

> ⚠️ **BUG FIX #1 (L2 dead-end):** Bản đầu chỉ nối `RefreshChipBreadcrumb` ở nhánh path-thật (else-else), 2 nhánh `__ALL__` và `""` dead-end sau `UpdateComboFolderHighlights` — phát hiện qua export K2Node. Đã nối cả 3 nhánh về 1 điểm chung.
>
> ⚠️ **BUG FIX #2 (Delimiter sai):** `Parse Into Array` trong `RefreshChipBreadcrumb` có `Delimiter = "/ "` (gạch chéo + khoảng trắng) thay vì `"/"` — gõ nhầm lúc tạo node. Vì `CurrentComboFolderPath` không chứa chuỗi `"/ "`, `ParseIntoArray` không tách được gì, trả mảng 1 phần tử → `Segments.Length <= 1` luôn True → hàm return sớm, chip area trống dù đáng lẽ phải vẽ. Đã sửa Delimiter về `"/"` (1 ký tự).
>
> ⚠️ **BUG FIX #3 (logic sai vô hại):** node điều kiện đầu `RefreshChipBreadcrumb` (check `__ALL__`/`""`) dùng nhầm `BooleanAND` thay vì `BooleanOR` — về lý thuyết sai (1 chuỗi không thể vừa bằng 2 giá trị khác nhau cùng lúc, nhánh True không bao giờ chạy được), nhưng KHÔNG gây triệu chứng vì `Branch(Segments.Length <= 1)` phía sau vô tình bắt được cả 2 case đó. Đã sửa `AND` → `OR` cho đúng ý định logic, dù hành vi runtime không đổi.

**Test PASS sau fix:** click __ALL__ dọn sạch chip / xóa folder cha đang xem con → về cha, con còn lại hiện đủ ngay / regression case 2-4 segment path đều đúng.

---

## 5. BUG FIX phát sinh — `SelectedPath` nhầm biến (class var trùng tên tham số)

**Phát hiện lúc test:** chọn `Livingroom/dinningtable` (cấp 2) → chip con hiện SAI, ra toàn bộ danh sách folder cấp gốc (giống hệt cột trái) thay vì đúng children của `dinningtable`.

**Nguyên nhân:** trong `OnComboTreeNodeClicked`, khi gọi `RebuildChipRowForPath(?, 1)`, Blueprint tự nối nhầm vào **GET class var `SelectedPath`** (biến cũ từ `PopulateTreeColumn` Furniture/Material side, cùng tên) thay vì đúng **pin `SelectedPath` của param Custom Event `OnComboTreeNodeClicked`**.

**Fix:** xóa dây từ GET class var, kéo lại từ đúng pin `Selected Path` trên node Entry `OnComboTreeNodeClicked` (không đổi tên biến nào, chỉ đổi nguồn dây nối).

> **Ghi chú cảnh báo cho AI_Implementation_Rules.md (backlog, không critical):** cân nhắc thêm rule/checklist "khi Custom Event có param trùng tên với 1 class var đã tồn tại trong Blueprint, kiểm tra kỹ dây nối trước khi coi flow đã đúng — Blueprint auto-suggest dễ nối nhầm vào biến sai khi 2 nguồn cùng tên."

**Test PASS sau fix:** click `Livingroom/dinningtable` → chỉ hiện đúng 2 chip con thật (`chair`, `ComboTableRound`), không lẫn cấp gốc.

---

## 6. Cập nhật Session_State.md / PROGRESS.md

### Session_State.md — TIẾP THEO, cập nhật mục 4-5-6
```
4. ✅ Nút "+" đầu cột tree (NF.G3) — DONE (06/07)
5. ✅ Xóa folder (C5.6) — DONE (06/07): WBP_ConfirmDialog mới + HandleDeleteFolderConfirmed
   (deviation D-C5.6-1: nhảy về folder CHA thay vì __ALL__)
6. 🔄 ChipTag right-click + rename (C5.7) — C5.7a DONE (06/07, right-click + context menu).
   C5.7b (inline rename trong chip, nhúng WBP_EditableLabel) CÒN NỢ — kế tiếp.
```
Sau C5.7b: → REG (regression 12 bước) → C5 HOÀN TẤT → C6/C7 (defer) hoặc WBP_Toast → C9.

### PROGRESS.md — cập nhật dòng C5
```
- [x] C5.6 — Xóa folder ✅ DONE (06/07) — WBP_ConfirmDialog mới + ClearFolderPrefix (đã có từ C5.1)
- [~] C5.7 — ChipTag right-click + rename ⏳ IN PROGRESS — 7a (right-click+menu) ✅ DONE (06/07),
      7b (inline rename) 🔲 CÒN NỢ
```

### DEVIATIONS.md — 3 dòng thêm
```
06/07 — D-C5.6-1: HandleDeleteFolderConfirmed đổi hành vi navigate sau xóa folder. Spec gốc
(test case 5, Combo_C5_FolderManagement_Plan_v2.md) quy định "nhảy về __ALL__". Đổi thành
"nhảy về folder CHA trực tiếp" (ParentOf, có sẵn từ C5.2) — nếu cha là gốc mới về __ALL__.
Lý do: UX mượt hơn khi xóa folder lồng sâu, giữ ngữ cảnh thay vì bật về Tất cả. cuhoang duyệt.

06/07 — Bug fix RefreshComboFolderUI: 2/3 nhánh filter (__ALL__, "") dead-end sau
UpdateComboFolderHighlights, không nối tới RefreshChipBreadcrumb (hàm mới) → chip area
không tự dọn khi navigate về __ALL__/"". Phát hiện qua export K2Node, đã nối cả 3 nhánh.

06/07 — Bug fix RefreshChipBreadcrumb: Delimiter ParseIntoArray gõ nhầm "/ " (có khoảng
trắng) thay vì "/" → Segments không tách được, chip area trống sau move/xóa folder con
trong path ≥2 cấp. Đã sửa Delimiter. Kèm sửa phụ: BooleanAND→BooleanOR ở guard đầu hàm
(logic sai nhưng vô hại runtime, sửa cho đúng ý định).
```

---

## 7. Danh sách file cần cập nhật (Claude Code checklist)

| File | Hành động |
|---|---|
| `docs/00_Core/01_Session_State.md` | Update mục TIẾP THEO (mục 4-5-6), version+giờ mới |
| `docs/00_Core/PROGRESS.md` | Update dòng C5.6, C5.7 |
| `docs/00_Core/DEVIATIONS.md` | Thêm 3 dòng mục 6 |
| `docs/Widgets/WBP_FurnitureInventory.md` | Bump version — thêm: PendingDeleteFolderPath, OnRequestDeleteFolder impl, HandleDeleteFolderConfirmed, CB_DeleteFolderClick impl, RebuildChipRowForPath (mới), RefreshChipBreadcrumb (mới), RefreshComboFolderUI (sửa — nối 3 nhánh), PopulateComboTreeColumn (thêm PlusNode), OnComboTreeNodeClicked (thêm guard sentinel + fix SelectedPath) |
| `docs/Widgets/WBP_ChipTag.md` | Tạo mới (nếu chưa tách khỏi WBP_FurnitureInventory.md) — thêm On Mouse Button Down override + OnChipRightClicked dispatcher |
| `docs/Widgets/WBP_ConfirmDialog.md` | Tạo mới |
| `docs/Blueprints/Blueprint_Logic_NodeFlow.md` | Thêm tóm tắt node flow NF.G3 guard, RebuildChipRowForPath, RefreshChipBreadcrumb |
| `Combo_C5_FolderManagement_Plan_v2.md` (project file) | Đánh dấu NF.G3, C5.6, C5.7a = ✅ DONE trong bảng ánh xạ mục 3 |

---

*Hết delta. cuhoang review diff trước khi commit như thường lệ (Luật handoff mục 13).*
