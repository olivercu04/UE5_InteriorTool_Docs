# DELTA — C5.8 Task Card #2 as-built (2a + 2b Part A + Part B lần 1)
**Version:** 1.0 — 11/07/2026 (giờ tạo: theo timestamp commit) · **Người tạo:** Fable (Opus), tổng hợp từ session 10-11/07
**Trạng thái nguồn:** 2a ✅ PASS · 2b Part A ✅ DONE (verify export XML) · Part B+2c 🔄 IN PROGRESS (Phần 1/2/4a/4d/4e + 2 handler DONE, bug #2 đang debug theo `C5.8_TaskCard2_FixPlan_11jul2026.md`)
**Task Card #1 (Data Layer):** ✅ đã phân phối 08/07 (`WBP_FurnitureInventory.md` v3.10, node flow thật từ K2Node export) — delta này KHÔNG đụng lại, chỉ ghi con trỏ.

**Quy ước cờ:** flow không đánh dấu = đã verify (export XML / Print test / trace tay). Flow gắn `⚠️ SUY LUẬN` = ghi theo hướng dẫn trong chat, CHƯA có K2Node export — thay bằng bản export khi cuhoang gửi (xem mục "EXPORT CẦN BỔ SUNG").

---

## A. `WBP_FolderPickerRow` — widget MỚI (tạo doc `WBP_FolderPickerRow.md` v1.0)

**Vai trò:** row của picker cây folder (Lớp 3 UI theo plan C5.8 §4), thay `WBP_MoveFolderRow`. KHÔNG đục `WBP_TreeNode` (fork Y đã chốt).

### Layout (2a)
```
HB_Row
├── TXT_Guide   (TextBlock — guide glyphs │ ├ └ dạng concatenated string, KHÔNG Image cột)
├── BTN_Arrow   (Button, bọc TXT_Arrow ▶/▼)
└── BTN_Name    (Button, bọc HB_Label [ TXT_Name + TXT_Badge ])
```
- Guide line render bằng **chuỗi ký tự Unicode nối sẵn** (deviation so plan gốc §4 "Image cột dọc/elbow" — lý do: `Create Widget` không instantiate native TextBlock dynamic; đã PASS 45+ folder). Ceiling: đủ tốt scale hiện tại; trigger: khi cần style guide line đậm/màu → chuyển Image.
- `TXT_Arrow` dùng Visibility **Hidden** (không Collapsed) khi `!HasChildren` — giữ căn lề.

### Variables
| Tên | Kiểu | Ghi chú |
|---|---|---|
| `RowNode` | `S_FolderTreeNode` | SET trong `SetNode`, mọi Broadcast đọc từ đây |

### Event Dispatchers (2b Part A)
```
OnRowExpandClicked(Path : String)
OnRowSelected(Path : String)
```

### `SetNode(Node : S_FolderTreeNode)` — Function (2a, SỬA ở 2b)
⚠️ SUY LUẬN phần đầu 2a (guide glyphs) — đoạn cuối as-built đã confirm trong chat:
```
▶ SET RowNode = Node                                  ← ⚠️ SUY LUẬN vị trí, confirm khi export
▶ (dựng guide string từ Node.ContinuesAncestors[] + Node.bIsLast → SetText TXT_Guide)  ⚠️ SUY LUẬN chi tiết
▶ Set Text(TXT_Name, To Text(Node.DisplayLabel))
▶ Set Visibility(TXT_Badge, Collapsed)                ← TRƯỚC Branch (cả 2 nhánh đều cần)
▶ Branch(Node.HasChildren)
   True  → Set Visibility(TXT_Arrow, Visible)
   False → Set Visibility(TXT_Arrow, Hidden)
```

### `SetExpanded(bExpanded : Boolean)` — Function (2b Part A, verify export XML)
```
▶ Set Text(TXT_Arrow, To Text( Select String(A="▼", B="▶", bPickA=bExpanded) ))
▶ Set Visibility(TXT_Badge, Collapsed)
▶ Branch( RowNode.HasChildren AND NOT bExpanded )
   True  → Set Visibility(TXT_Badge, Visible)
           Set Text(TXT_Badge, To Text( Concat("(", Concat(Conv_IntToString(RowNode.ChildCount), ")")) ))
   False → [dead-end hợp lệ]
```

### Button handlers (2b Part A — **ĐÍNH CHÍNH quan trọng**)
As-built dùng **Custom Event trung gian** (sinh từ Details panel "+"), KHÁC ghi chú TIẾN ĐỘ trong `C5.8_TaskCard2_PartB_2c_10jul2026.md` ("nối THẲNG vào Call dispatcher"):
```
BTN_Arrow.OnClicked → HandleArrowClicked (Custom Event)
   ▶ Broadcast OnRowExpandClicked(RowNode.Path)
BTN_Name.OnClicked  → HandleNameClicked (Custom Event)
   ▶ Broadcast OnRowSelected(RowNode.Path)
```

### Còn nợ (Task Card sau)
`SetSearchHighlight(bMatch)` (Part 2c — chưa build) · `EnterRenameMode`/host `WBP_EditableLabel` (2d).

---

## B. `WBP_FolderTreePicker` — widget MỚI (tạo doc `WBP_FolderTreePicker.md` v0.9 — IN PROGRESS)

**Vai trò:** Lớp 2 SHARED component (plan C5.8 §4), nhúng vào 2 vỏ Move/Save (chưa wire — §7.3/7.4 sau).

### Layout (2a + Part B)
```
VB_Picker
├── HB_Toolbar
│   ├── BTN_ExpandAll   → TextBlock "Mở tất cả"
│   └── BTN_CollapseAll → TextBlock "Thu gọn"
├── SB_SearchFolder     ← Search Bar, FULL-WIDTH hàng riêng (đúng §4 plan gốc)
└── SB_Rows             (ScrollBox chứa WBP_FolderPickerRow)
```

### Variables (Part B)
| Tên | Kiểu | Default | Ghi chú |
|---|---|---|---|
| `Folders` | Array\<S_FolderTreeNode\> | rỗng | push từ ngoài qua `SetFolders` (F3a — picker KHÔNG tự đọc storage) |
| `ExpandedFolders` | **Array\<String\>** | rỗng | deviation `[ARCH]` — Array thay Set (plan gốc ghi Set) |
| `SelectedPath` | String | "" | chưa dùng highlight |
| `bIsSearching` | Boolean | false | |
| `SearchExpandOverride` | Array\<String\> | rỗng | tập force-expand lúc search, KHÔNG đụng ExpandedFolders |

### Event Dispatcher
```
OnFolderSelected(Path : String)
```

### `IsPathVisible(Path : String) → Boolean` — Function (DONE, trace tay export XML khớp 100%)
Node hiện ⇔ mọi tổ tiên ∈ `ExpandedFolders`. Top-level / "(Gốc)" luôn True.
Flow theo §4a task card (Segments/CurrentPrefix/bHidden, loop break tại index cuối, Completed → Return(NOT bHidden)).
⚠️ Chi tiết loop as-built (ForEachLoopWithBreak hay For Loop With Break) — lấy theo K2Node export khi cuhoang gửi; kết quả Print test đã PASS (top-level=True, tổ tiên chưa expand=False, nested-chain sâu đúng).

### `RefreshVisibleRows()` — Function (DONE — SINGLE SOURCE expand-mode + search-mode)
⚠️ SUY LUẬN theo §4d task card (nhánh search hiện để `bIsSearching=False` mặc định, chưa ghép search):
```
▶ SET QueryStr = Get Text(SB_SearchFolder) → ToString
▶ Clear Children(SB_Rows)
▶ ForEachLoop(Folders):
     Branch(bIsSearching)
        True  → bShow = Array_Contains(SearchExpandOverride, node.Path) OR PathMatchesQuery(...)
        False → bShow = IsPathVisible(node.Path)
     Branch(bShow) True →
        Create Widget(WBP_FolderPickerRow) → Row
        Row.SetNode(node)
        Branch(bIsSearching): True → SetExpanded(True)+SetSearchHighlight(match)
                              False → SetExpanded(Array_Contains(ExpandedFolders,node.Path))+SetSearchHighlight(False)*
        Bind Row.OnRowExpandClicked → HandleRowExpandClicked
        Bind Row.OnRowSelected → HandleRowSelected
        Add Child(SB_Rows, Row)
   Completed → hết hàm
```
(*) `SetSearchHighlight` chưa tồn tại ở lần build 1 — nhánh này chưa có node đó.

### `SetFolders(Nodes)` — SỬA (bug đã fix, ghi lesson)
Thân 2a cũ (tự loop tạo row) → thay bằng:
```
▶ SET Folders = Nodes
▶ RefreshVisibleRows()
```
**Bug lesson:** Sonnet quên nhắc sửa theo bước 4e task card → breakpoint không fire. Phát hiện + fix bởi cuhoang, verify Print `IsPathVisible` PASS.

### Custom Events (DONE, ⚠️ SUY LUẬN theo §5 task card)
```
HandleRowExpandClicked(Path): Branch(Array_Contains(ExpandedFolders,Path))
   True→Remove Item · False→Add Unique · [merge] → RefreshVisibleRows()
HandleRowSelected(Path): SET SelectedPath=Path → Broadcast OnFolderSelected(Path)
```

### Chưa build (Part 2c còn lại)
`PathMatchesQuery` · `BuildSearchOverride` · nhánh search trong Refresh · `SB_SearchFolder.OnSearchTextChanged` · `BTN_ExpandAll/CollapseAll.OnClicked`.

### Test status
Mục 1 (top-level only) PASS · **mục 2 FAIL (bug #2, đang debug theo FixPlan)** · mục 3-10 chưa chạy.

---

## C. DEVIATIONS.md — 4 entry mới

1. `[ARCH] ExpandedFolders: Array<String> thay Set<String>` — lý do: tránh node Set chưa xác nhận, project chưa dùng Set trong BP. Ceiling: vài chục folder; trigger: scale nghìn folder → đổi Set.
2. `[SWAP] ETB_Search → SB_SearchFolder (Search Bar)` — lý do: tiền lệ Material search trong WBP_FurnitureInventory (có `OnSearchTextChanged` + pin Text), tránh bug CommonSearchBox cũ. Ceiling/trigger: n/a.
3. `[DOC-FIX] 2b Part A as-built dùng Custom Event trung gian HandleArrowClicked/HandleNameClicked` — ghi chú "nối THẲNG vào Call dispatcher" trong `C5.8_TaskCard2_PartB_2c_10jul2026.md` mục TIẾN ĐỘ là SAI → sửa dòng đó trong task card.
4. `[LESSON] SetFolders giữ thân cũ 2a khi build Part B` — quy tắc mới đề xuất: sửa function CÓ SẴN phải diff thân cũ trước, không chỉ thêm mới (candidate vào AI_Implementation_Rules).

Guide line bằng Unicode string (mục A) — deviation này đã log từ 2a; nếu chưa có trong DEVIATIONS.md thì thêm.

---

## D. EXPORT CẦN BỔ SUNG (cuhoang dán K2Node trước hoặc sau khi gửi Claude Code)

Blueprint Export Method (select nodes → Ctrl+C → dán):
1. `WBP_FolderPickerRow.SetNode` (full, gồm guide string build)
2. `WBP_FolderTreePicker.IsPathVisible` (đã trace tay — export để thay cờ ⚠️)
3. `WBP_FolderTreePicker.RefreshVisibleRows`
4. `HandleRowExpandClicked` + `HandleRowSelected`

Chưa có export → Claude Code giữ cờ `⚠️ SUY LUẬN` trong doc (đúng tiền lệ 08/07 với Task Card #1).

---

## E. COMMAND BLOCK cho Claude Code

```
Nhiệm vụ: phân phối delta C5.8_TaskCard2_Delta_asbuilt_11jul2026.md vào canonical docs.
KP3: chỉ đụng đúng các file dưới, không refactor gì khác.

1. TẠO MỚI docs/Blueprints/WBP_FolderPickerRow.md (v1.0, 11/07/2026 + giờ phút)
   — nội dung mục A (trigger "tạo khi Task Card #2 thực thi" trong C5.8 plan §Ghi chú phân phối đã tới).
2. TẠO MỚI docs/Blueprints/WBP_FolderTreePicker.md (v0.9 — IN PROGRESS, 11/07/2026)
   — nội dung mục B, giữ nguyên các cờ ⚠️ SUY LUẬN.
3. DEVIATIONS.md — thêm 4 entry mục C (ngày 11/07/2026).
4. C5.8_TaskCard2_PartB_2c_10jul2026.md — sửa dòng TIẾN ĐỘ 2b Part A theo [DOC-FIX] mục C.3.
5. 01_Session_State.md — cập nhật: Task Card #2 Part B đang IN PROGRESS,
   DONE: Phần 1/2, IsPathVisible, RefreshVisibleRows, SetFolders fix, 2 handler;
   ĐANG: bug #2 (test mục 2) — debug theo docs/Sprints/Sprint5/C5.8_TaskCard2_FixPlan_11jul2026.md;
   TIẾP THEO trỏ về FixPlan Giai đoạn 0.
6. PROGRESS.md — thêm 1 dòng lịch sử 10-11/07: 2a ✅, 2b Part A ✅, Part B lần 1 (bug #2 open).
7. CHÉP docs/Sprints/Sprint5/C5.8_TaskCard2_FixPlan_11jul2026.md vào repo (file mới).
8. C5.8_FolderTreePicker_Unify_Plan.md — mục Ghi chú phân phối: thêm dòng 11/07 (UI docs đã tạo).
Mỗi file sửa: cập nhật version + ngày giờ phút. Xuất diff cho cuhoang review trước khi commit.
```
